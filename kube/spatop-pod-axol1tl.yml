apiVersion: v1
kind: Pod
metadata:
  name: ts-spatop-pod-axol1tl
spec:
  securityContext:
    runAsUser: 0
    fsGroup: 0
  containers:
  - name: gpu-container
    image:  gitlab-registry.nrp-nautilus.io/jmduarte/hhh:latest
    command: ["/bin/bash", "-c", "sleep infinity"]
    stdin: true
    tty: true
    resources:
      limits:
        nvidia.com/gpu: 1
        memory: 12Gi
        cpu: 2
      requests:
        nvidia.com/gpu: 1
        memory: 10Gi
        cpu: 1
    volumeMounts:
    - mountPath: /data
      name: spatopvol
  restartPolicy: Never
  volumes:
    - name: spatopvol
      persistentVolumeClaim:
        claimName: traindatavol
  tolerations:
    - key: "nautilus.io/axol1tl"
      operator: "Exists"
      effect: "NoSchedule"
